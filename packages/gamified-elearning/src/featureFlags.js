// Feature flags — flip USE_ARTISTIC_HOMEPAGE to false to instantly revert to the old homepage/header.
// No other code changes required; App.js reads this flag at import time.

export const USE_ARTISTIC_HOMEPAGE = true;
